{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AnsiblePlaybook",
  "description": "Manages an Ansible Playbook — the top-level execution unit.",
  "typeName": "Ansible::Execution::Playbook",
  "domain": "Execution",
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the Playbook."
    },
    "become": {
      "type": "boolean",
      "description": "Whether to run tasks with privilege escalation (sudo) by default."
    }
  },
  "required": ["name"],
  "primaryIdentifier": ["/properties/name"]
}
