{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AnsiblePlay",
  "description": "Manages an Ansible Play — groups tasks targeting a host group.",
  "typeName": "Ansible::Execution::Play",
  "domain": "Execution",
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the Play."
    },
    "playbookId": {
      "type": "string",
      "description": "Reference to the parent Playbook."
    },
    "hosts": {
      "type": "string",
      "description": "Host pattern or group name this play targets."
    },
    "become": {
      "type": "boolean",
      "description": "Whether to run tasks with privilege escalation (sudo) by default."
    },
    "gatherFacts": {
      "type": "boolean",
      "description": "Whether to gather facts about the target hosts before running tasks."
    }
  },
  "required": ["name", "playbookId", "hosts"],
  "primaryIdentifier": ["/properties/name"]
}
