{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MultipassConfig",
  "description": "Multipass configuration root. Acts as the parent resource that groups MltInstance resources into a single synthesized YAML file.",
  "typeName": "Multipass::Compute::Config",
  "domain": "Compute",
  "additionalProperties": false,
  "properties": {
    "instances": {
      "type": "array",
      "items": { "type": "object" },
      "description": "VM instances to include. Each entry is a reference to an MltInstance resource via resource.ref."
    }
  },
  "readOnlyProperties": ["/properties/configId"],
  "primaryIdentifier": ["/properties/configId"]
}
