{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MultipassMount",
  "description": "A host-to-guest directory mount that can be referenced by one or more MltInstance resources.",
  "typeName": "Multipass::VM::Mount",
  "domain": "VM",
  "additionalProperties": false,
  "properties": {
    "source": {
      "type": "string",
      "description": "Absolute path on the host machine."
    },
    "target": {
      "type": "string",
      "description": "Absolute path inside the guest VM."
    }
  },
  "required": ["source"]
}
