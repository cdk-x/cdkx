{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MultipassNetwork",
  "description": "A Multipass network interface that can be referenced by one or more MltInstance resources.",
  "typeName": "Multipass::VM::Network",
  "domain": "VM",
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the host network interface (e.g. 'bridge', 'en0')."
    },
    "mode": {
      "type": "string",
      "enum": ["auto", "manual"],
      "description": "Network mode."
    },
    "mac": {
      "type": "string",
      "description": "Optional MAC address override."
    }
  },
  "required": ["name"]
}
