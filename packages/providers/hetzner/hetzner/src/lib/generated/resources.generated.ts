// ============================================================
// AUTO-GENERATED — DO NOT EDIT
//
// Generated by spec-to-cdkx from Hetzner JSON Schema files.
// To regenerate: yarn nx run @cdkx-io/hetzner:codegen
// ============================================================

import { ProviderResource, IResolvable, PropertyValue } from '@cdkx-io/core';
import { Construct } from 'constructs';

/**
 * All Hetzner resource type identifiers, grouped by domain.
 *
 * Use these constants when constructing L1 resources to avoid typos.
 */
export const HetznerResourceType = {
  /** Security resources. */
  Security: {
    /** `Hetzner::Security::Certificate` */
    Certificate: 'Hetzner::Security::Certificate',
    /** `Hetzner::Security::Firewall` */
    Firewall: 'Hetzner::Security::Firewall',
    /** `Hetzner::Security::SshKey` */
    SshKey: 'Hetzner::Security::SshKey',
  },
  /** Networking resources. */
  Networking: {
    /** `Hetzner::Networking::FloatingIp` */
    FloatingIp: 'Hetzner::Networking::FloatingIp',
    /** `Hetzner::Networking::Network` */
    Network: 'Hetzner::Networking::Network',
    /** `Hetzner::Networking::PrimaryIp` */
    PrimaryIp: 'Hetzner::Networking::PrimaryIp',
    /** `Hetzner::Networking::Route` */
    Route: 'Hetzner::Networking::Route',
    /** `Hetzner::Networking::Subnet` */
    Subnet: 'Hetzner::Networking::Subnet',
  },
  /** Compute resources. */
  Compute: {
    /** `Hetzner::Compute::LoadBalancer` */
    LoadBalancer: 'Hetzner::Compute::LoadBalancer',
    /** `Hetzner::Compute::NetworkAttachment` */
    NetworkAttachment: 'Hetzner::Compute::NetworkAttachment',
    /** `Hetzner::Compute::PlacementGroup` */
    PlacementGroup: 'Hetzner::Compute::PlacementGroup',
    /** `Hetzner::Compute::Server` */
    Server: 'Hetzner::Compute::Server',
  },
  /** Storage resources. */
  Storage: {
    /** `Hetzner::Storage::Volume` */
    Volume: 'Hetzner::Storage::Volume',
  }
} as const;

// ==============================================================================
// Common
// ==============================================================================

// Shared definitions referenced by multiple resources.
/**
 * Hetzner Cloud physical location.
 */
export enum Location {
  /** `fsn1` */
  FSN1 = 'fsn1',
  /** `nbg1` */
  NBG1 = 'nbg1',
  /** `hel1` */
  HEL1 = 'hel1',
  /** `ash` */
  ASH = 'ash',
  /** `hil` */
  HIL = 'hil',
  /** `sin` */
  SIN = 'sin',
}

/**
 * Hetzner Cloud network zone.
 */
export enum NetworkZone {
  /** `eu-central` */
  EU_CENTRAL = 'eu-central',
  /** `us-east` */
  US_EAST = 'us-east',
  /** `us-west` */
  US_WEST = 'us-west',
  /** `ap-southeast` */
  AP_SOUTHEAST = 'ap-southeast',
}


// ==============================================================================
// Security
// ==============================================================================

// --- Certificate ---
/**
 * Choose between uploading a Certificate in PEM format or requesting a managed Let's Encrypt Certificate.
 */
export enum CertificateType {
  /** `uploaded` */
  UPLOADED = 'uploaded',
  /** `managed` */
  MANAGED = 'managed',
}

/**
 * Props for {@link HtzCertificate}.
 *
 * Manages a Hetzner Cloud Certificate.
 */
export interface HetznerCertificate {
  /**
   * Name of the Certificate.
   */
  name: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Choose between uploading a Certificate in PEM format or requesting a managed Let's Encrypt Certificate.
   */
  type?: CertificateType;
  /**
   * Certificate and chain in PEM format, in order so that each record directly certifies the one preceding. Required for type `uploaded` Certificates.
   */
  certificate?: string;
  /**
   * Certificate key in PEM format. Required for type `uploaded` Certificates.
   */
  privateKey?: string;
  /**
   * Domains and subdomains that should be contained in the Certificate issued by Let's Encrypt. Required for type `managed` Certificates.
   */
  domainNames?: (string | IResolvable)[];
}

/**
 * L1 construct for a Hetzner Certificate resource.
 *
 * Manages a Hetzner Cloud Certificate.
 */
export class HtzCertificate extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Security::Certificate';

  /**
   * The `certificateId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'certificateId' }` at synthesis time.
   */
  public readonly attrCertificateId: IResolvable;

  public name: string;
  public labels?: Record<string, string>;
  public resourceType?: CertificateType;
  public certificate?: string;
  public privateKey?: string;
  public domainNames?: (string | IResolvable)[];

  constructor(scope: Construct, id: string, props: HetznerCertificate) {
    super(scope, id, {
      type: HtzCertificate.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrCertificateId = this.getAtt('certificateId');
    this.name = props.name;
    this.labels = props.labels;
    this.resourceType = props.type;
    this.certificate = props.certificate;
    this.privateKey = props.privateKey;
    this.domainNames = props.domainNames;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      name: this.name,
      labels: this.labels,
      type: this.resourceType,
      certificate: this.certificate,
      privateKey: this.privateKey,
      domainNames: this.domainNames,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- Firewall ---
/**
 * Array of rules. Rules are limited to 50 entries per Firewall and 500 effective rules.
 */
export interface FirewallRule {
  /**
   * Description of the rule.
   */
  description?: string | null;
  /**
   * Traffic direction in which the rule should be applied to.
   */
  direction: FirewallRuleDirection;
  /**
   * List of permitted IPv4/IPv6 addresses for incoming traffic.
   */
  sourceIps?: (string | IResolvable)[];
  /**
   * List of permitted IPv4/IPv6 addresses for outgoing traffic.
   */
  destinationIps?: (string | IResolvable)[];
  /**
   * Network protocol to apply the rule for.
   */
  protocol: FirewallRuleProtocol;
  /**
   * Port or port range to apply the rule for.
   */
  port?: string;
}

/**
 * Server the Firewall is applied to. Only set for type `server`, otherwise `null`.
 */
export interface FirewallApplyToServer {
  /**
   * ID of the Server.
   */
  id: number;
}

/**
 * Label Selector the Firewall is applied to. Only set for type `label_selector`, otherwise `null`.
 */
export interface FirewallApplyToLabelSelector {
  /**
   * The selector.
   */
  selector: string;
}

/**
 * Resources to apply the Firewall to. Resources added directly are taking precedence over those added via a Label Selector.
 */
export interface FirewallApplyTo {
  /**
   * Type of the resource.
   */
  type: FirewallApplyToType;
  /**
   * Server the Firewall is applied to.
   */
  server?: FirewallApplyToServer;
  /**
   * Label Selector the Firewall is applied to.
   */
  labelSelector?: FirewallApplyToLabelSelector;
}

/**
 * Traffic direction in which the rule should be applied to. Use `source_ips` for direction `in` and `destination_ips` for direction `out` to specify IPs.
 */
export enum FirewallRuleDirection {
  /** `in` */
  IN = 'in',
  /** `out` */
  OUT = 'out',
}

/**
 * Network protocol to apply the rule for.
 */
export enum FirewallRuleProtocol {
  /** `tcp` */
  TCP = 'tcp',
  /** `udp` */
  UDP = 'udp',
  /** `icmp` */
  ICMP = 'icmp',
  /** `esp` */
  ESP = 'esp',
  /** `gre` */
  GRE = 'gre',
}

/**
 * Type of the resource.
 */
export enum FirewallApplyToType {
  /** `server` */
  SERVER = 'server',
  /** `label_selector` */
  LABEL_SELECTOR = 'label_selector',
}

/**
 * Props for {@link HtzFirewall}.
 *
 * Manages a Hetzner Cloud Firewall.
 */
export interface HetznerFirewall {
  /**
   * Name of the Firewall.
   */
  name: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Array of rules.
   */
  rules?: FirewallRule[];
  /**
   * Resources to apply the Firewall to.
   */
  applyTo?: FirewallApplyTo[];
}

/**
 * L1 construct for a Hetzner Firewall resource.
 *
 * Manages a Hetzner Cloud Firewall.
 */
export class HtzFirewall extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Security::Firewall';

  /**
   * The `firewallId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'firewallId' }` at synthesis time.
   */
  public readonly attrFirewallId: IResolvable;

  public name: string;
  public labels?: Record<string, string>;
  public rules?: FirewallRule[];
  public applyTo?: FirewallApplyTo[];

  constructor(scope: Construct, id: string, props: HetznerFirewall) {
    super(scope, id, {
      type: HtzFirewall.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrFirewallId = this.getAtt('firewallId');
    this.name = props.name;
    this.labels = props.labels;
    this.rules = props.rules;
    this.applyTo = props.applyTo;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      name: this.name,
      labels: this.labels,
      rules: this.rules,
      applyTo: this.applyTo,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- SshKey ---
/**
 * Props for {@link HtzSshKey}.
 *
 * Manages a Hetzner Cloud SSH Key.
 */
export interface HetznerSshKey {
  /**
   * Name of the SSH key.
   */
  name: string;
  /**
   * Public key.
   */
  publicKey: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
}

/**
 * L1 construct for a Hetzner SshKey resource.
 *
 * Manages a Hetzner Cloud SSH Key.
 */
export class HtzSshKey extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Security::SshKey';

  /**
   * The `name` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'name' }` at synthesis time.
   */
  public readonly attrName: IResolvable;

  public name: string;
  public publicKey: string;
  public labels?: Record<string, string>;

  constructor(scope: Construct, id: string, props: HetznerSshKey) {
    super(scope, id, {
      type: HtzSshKey.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrName = this.getAtt('name');
    this.name = props.name;
    this.publicKey = props.publicKey;
    this.labels = props.labels;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      name: this.name,
      publicKey: this.publicKey,
      labels: this.labels,
    } as unknown as Record<string, PropertyValue>;
  }
}


// ==============================================================================
// Networking
// ==============================================================================

// --- FloatingIp ---
/**
 * The Floating IP type.
 */
export enum FloatingIpType {
  /** `ipv4` */
  IPV4 = 'ipv4',
  /** `ipv6` */
  IPV6 = 'ipv6',
}

/**
 * Props for {@link HtzFloatingIp}.
 *
 * Manages a Hetzner Cloud Floating IP.
 */
export interface HetznerFloatingIp {
  /**
   * The Floating IP type.
   */
  type: FloatingIpType;
  /**
   * ID of the Server the Floating IP is assigned to.
   */
  serverId?: number | IResolvable | null;
  /**
   * Home Location for the Floating IP.
   */
  homeLocation?: Location;
  /**
   * Description of the Resource.
   */
  description?: string | null;
  /**
   * Name of the Resource. Must be unique per Project.
   */
  name?: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
}

/**
 * L1 construct for a Hetzner FloatingIp resource.
 *
 * Manages a Hetzner Cloud Floating IP.
 */
export class HtzFloatingIp extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Networking::FloatingIp';

  /**
   * The `floatingIpId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'floatingIpId' }` at synthesis time.
   */
  public readonly attrFloatingIpId: IResolvable;

  public resourceType: FloatingIpType;
  public serverId?: number | IResolvable | null;
  public homeLocation?: Location;
  public description?: string | null;
  public name?: string;
  public labels?: Record<string, string>;

  constructor(scope: Construct, id: string, props: HetznerFloatingIp) {
    super(scope, id, {
      type: HtzFloatingIp.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrFloatingIpId = this.getAtt('floatingIpId');
    this.resourceType = props.type;
    this.serverId = props.serverId;
    this.homeLocation = props.homeLocation;
    this.description = props.description;
    this.name = props.name;
    this.labels = props.labels;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      type: this.resourceType,
      serverId: this.serverId,
      homeLocation: this.homeLocation,
      description: this.description,
      name: this.name,
      labels: this.labels,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- Network ---
/**
 * Props for {@link HtzNetwork}.
 *
 * Manages a Hetzner Cloud private Network.
 */
export interface HetznerNetwork {
  /**
   * Name of the Network.
   */
  name: string;
  /**
   * IP range of the Network in CIDR notation. Must be a private IPv4 range per RFC1918. Minimum /24, recommended /16.
   */
  ipRange: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Toggle to expose routes to the Networks vSwitch.
   */
  exposeRoutesToVswitch?: boolean;
}

/**
 * L1 construct for a Hetzner Network resource.
 *
 * Manages a Hetzner Cloud private Network.
 */
export class HtzNetwork extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Networking::Network';

  /**
   * The `networkId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'networkId' }` at synthesis time.
   */
  public readonly attrNetworkId: IResolvable;

  public name: string;
  public ipRange: string;
  public labels?: Record<string, string>;
  public exposeRoutesToVswitch?: boolean;

  constructor(scope: Construct, id: string, props: HetznerNetwork) {
    super(scope, id, {
      type: HtzNetwork.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrNetworkId = this.getAtt('networkId');
    this.name = props.name;
    this.ipRange = props.ipRange;
    this.labels = props.labels;
    this.exposeRoutesToVswitch = props.exposeRoutesToVswitch;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      name: this.name,
      ipRange: this.ipRange,
      labels: this.labels,
      exposeRoutesToVswitch: this.exposeRoutesToVswitch,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- PrimaryIp ---
/**
 * Primary IP type.
 */
export enum PrimaryIpType {
  /** `ipv4` */
  IPV4 = 'ipv4',
  /** `ipv6` */
  IPV6 = 'ipv6',
}

/**
 * Type of resource the Primary IP can get assigned to. Currently Primary IPs can only be assigned to Servers, therefore this field must be set to `server`.
 */
export enum PrimaryIpAssigneeType {
  /** `server` */
  SERVER = 'server',
}

/**
 * Props for {@link HtzPrimaryIp}.
 *
 * Manages a Hetzner Cloud Primary IP.
 */
export interface HetznerPrimaryIp {
  /**
   * ID of the Primary IP. Assigned by the API on creation.
   */
  id?: number;
  /**
   * Name of the Resource. Must be unique per Project.
   */
  name: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Primary IP type.
   */
  type: PrimaryIpType;
  /**
   * Location the Primary IP will be bound to.
   */
  location?: Location;
  /**
   * Type of resource the Primary IP can get assigned to.
   */
  assigneeType: PrimaryIpAssigneeType;
  /**
   * ID of resource to assign the Primary IP to.
   */
  assigneeId?: number | IResolvable | null;
  /**
   * Auto deletion state.
   */
  autoDelete?: boolean;
}

/**
 * L1 construct for a Hetzner PrimaryIp resource.
 *
 * Manages a Hetzner Cloud Primary IP.
 */
export class HtzPrimaryIp extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Networking::PrimaryIp';

  /**
   * The `id` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'id' }` at synthesis time.
   */
  public readonly attrId: IResolvable;

  public id?: number;
  public name: string;
  public labels?: Record<string, string>;
  public resourceType: PrimaryIpType;
  public location?: Location;
  public assigneeType: PrimaryIpAssigneeType;
  public assigneeId?: number | IResolvable | null;
  public autoDelete?: boolean;

  constructor(scope: Construct, id: string, props: HetznerPrimaryIp) {
    super(scope, id, {
      type: HtzPrimaryIp.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrId = this.getAtt('id');
    this.id = props.id;
    this.name = props.name;
    this.labels = props.labels;
    this.resourceType = props.type;
    this.location = props.location;
    this.assigneeType = props.assigneeType;
    this.assigneeId = props.assigneeId;
    this.autoDelete = props.autoDelete;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      id: this.id,
      name: this.name,
      labels: this.labels,
      type: this.resourceType,
      location: this.location,
      assigneeType: this.assigneeType,
      assigneeId: this.assigneeId,
      autoDelete: this.autoDelete,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- Route ---
/**
 * Props for {@link HtzRoute}.
 *
 * Manages a route added to an existing Hetzner Cloud Network via the add_route action.
 */
export interface HetznerRoute {
  /**
   * ID of the Network to add the route to.
   */
  networkId: number | IResolvable;
  /**
   * Destination network or host of the route in CIDR notation (e.g. '10.100.1.0/24').
   */
  destination: string;
  /**
   * Gateway IP address for the route (e.g. '10.0.1.1'). Cannot be the first IP of the Network's ip_range or 172.31.1.1.
   */
  gateway: string;
}

/**
 * L1 construct for a Hetzner Route resource.
 *
 * Manages a route added to an existing Hetzner Cloud Network via the add_route action.
 */
export class HtzRoute extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Networking::Route';

  public networkId: number | IResolvable;
  public destination: string;
  public gateway: string;

  constructor(scope: Construct, id: string, props: HetznerRoute) {
    super(scope, id, {
      type: HtzRoute.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.networkId = props.networkId;
    this.destination = props.destination;
    this.gateway = props.gateway;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      networkId: this.networkId,
      destination: this.destination,
      gateway: this.gateway,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- Subnet ---
/**
 * Type of subnet.
 */
export enum NetworkSubnetType {
  /** `cloud` */
  CLOUD = 'cloud',
  /** `server` */
  SERVER = 'server',
  /** `vswitch` */
  VSWITCH = 'vswitch',
}

/**
 * Props for {@link HtzSubnet}.
 *
 * Manages a subnet added to an existing Hetzner Cloud Network via the add_subnet action.
 */
export interface HetznerSubnet {
  /**
   * ID of the Network to add the subnet to.
   */
  networkId: number | IResolvable;
  /**
   * Type of subnet.
   */
  type: NetworkSubnetType;
  /**
   * Name of the Network Zone.
   */
  networkZone: NetworkZone;
  /**
   * IP range of the subnet in CIDR notation. If omitted, a /24 within the Network's ip_range is automatically allocated.
   */
  ipRange?: string;
  /**
   * ID of the robot vSwitch. Required when type is 'vswitch'.
   */
  vswitchId?: number | IResolvable;
}

/**
 * L1 construct for a Hetzner Subnet resource.
 *
 * Manages a subnet added to an existing Hetzner Cloud Network via the add_subnet action.
 */
export class HtzSubnet extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Networking::Subnet';

  public networkId: number | IResolvable;
  public resourceType: NetworkSubnetType;
  public networkZone: NetworkZone;
  public ipRange?: string;
  public vswitchId?: number | IResolvable;

  constructor(scope: Construct, id: string, props: HetznerSubnet) {
    super(scope, id, {
      type: HtzSubnet.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.networkId = props.networkId;
    this.resourceType = props.type;
    this.networkZone = props.networkZone;
    this.ipRange = props.ipRange;
    this.vswitchId = props.vswitchId;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      networkId: this.networkId,
      type: this.resourceType,
      networkZone: this.networkZone,
      ipRange: this.ipRange,
      vswitchId: this.vswitchId,
    } as unknown as Record<string, PropertyValue>;
  }
}


// ==============================================================================
// Compute
// ==============================================================================

// --- LoadBalancer ---
/**
 * Algorithm of the Load Balancer.
 */
export interface LoadBalancerAlgorithm {
  /**
   * Type of the algorithm.
   */
  type: LoadBalancerAlgorithmType;
}

/**
 * Additional configuration for protocol http.
 */
export interface LoadBalancerServiceHealthCheckHttp {
  /**
   * Host header to send in the HTTP request. May not contain spaces, percent or backslash symbols. Can be null, in that case no host header is sent.
   */
  domain: string | null;
  /**
   * HTTP path to use for health checks. May not contain literal spaces, use percent-encoding instead.
   */
  path: string;
  /**
   * String that must be contained in HTTP response in order to pass the health check.
   */
  response?: string;
  /**
   * List of returned HTTP status codes in order to pass the health check. Supports the wildcards `?` for exactly one character and `*` for multiple ones.
   */
  statusCodes?: (string | IResolvable)[];
  /**
   * Use HTTPS for health check.
   */
  tls?: boolean;
}

/**
 * Service health check.
 */
export interface LoadBalancerServiceHealthCheck {
  /**
   * Type of the health check.
   */
  protocol: LoadBalancerServiceHealthCheckProtocol;
  /**
   * Port the health check will be performed on.
   */
  port: number;
  /**
   * Time interval in seconds health checks are performed.
   */
  interval: number;
  /**
   * Time in seconds after an attempt is considered a timeout.
   */
  timeout: number;
  /**
   * Unsuccessful retries needed until a target is considered unhealthy; an unhealthy target needs the same number of successful retries to become healthy again.
   */
  retries: number;
  /**
   * Additional configuration for protocol http.
   */
  http?: LoadBalancerServiceHealthCheckHttp;
}

/**
 * Configuration option for protocols http and https.
 */
export interface LoadBalancerServiceHttp {
  /**
   * Name of the cookie used for sticky sessions.
   */
  cookieName?: string;
  /**
   * Lifetime of the cookie used for sticky sessions (in seconds).
   */
  cookieLifetime?: number;
  /**
   * IDs of the Certificates to use for TLS/SSL termination by the Load Balancer; empty for TLS/SSL passthrough or if `protocol` is `http`.
   */
  certificates?: (number | IResolvable)[];
  /**
   * Redirect HTTP requests to HTTPS. Only available if `protocol` is `https`.
   */
  redirectHttp?: boolean;
  /**
   * Use sticky sessions. Only available if `protocol` is `http` or `https`.
   */
  stickySessions?: boolean;
}

/**
 * Array of services.
 */
export interface LoadBalancerService {
  /**
   * Protocol of the Load Balancer.
   */
  protocol: LoadBalancerServiceProtocol;
  /**
   * Port the Load Balancer listens on.
   */
  listenPort: number;
  /**
   * Port the Load Balancer will balance to.
   */
  destinationPort: number;
  /**
   * Is Proxyprotocol enabled or not.
   */
  proxyprotocol: boolean;
  /**
   * Service health check.
   */
  healthCheck: LoadBalancerServiceHealthCheck;
  /**
   * Configuration option for protocols http and https.
   */
  http?: LoadBalancerServiceHttp;
}

/**
 * Configuration for type Server, only valid and required if type is `server`.
 */
export interface LoadBalancerTargetServer {
  /**
   * ID of the Server.
   */
  serverId: number | IResolvable;
}

/**
 * Configuration for label selector targets, only valid and required if type is `label_selector`.
 */
export interface LoadBalancerTargetLabelSelector {
  /**
   * Label selector.
   */
  selector: string;
}

/**
 * Configuration for an IP target. It is only possible to use the (Public or vSwitch) IPs of Hetzner Online Root Servers belonging to the project owner. IPs belonging to other users are blocked. Additionally IPs belonging to services provided by Hetzner Cloud (Servers, Load Balancers, ...) are blocked as well. Only valid and required if type is `ip`.
 */
export interface LoadBalancerTargetIp {
  /**
   * IP of a server that belongs to the same customer (public IPv4/IPv6) or private IP in a subnet type vswitch.
   */
  ip: string;
}

/**
 * Array of targets.
 */
export interface LoadBalancerTarget {
  /**
   * Type of the resource.
   */
  type: LoadBalancerTargetType;
  /**
   * Configuration for type Server, only valid and required if type is `server`.
   */
  server?: LoadBalancerTargetServer;
  /**
   * Use the private network IP instead of the public IP of the Server, requires the Server and Load Balancer to be in the same network. Only valid for target types `server` and `label_selector`.
   */
  usePrivateIp?: boolean;
  /**
   * Configuration for label selector targets, only valid and required if type is `label_selector`.
   */
  labelSelector?: LoadBalancerTargetLabelSelector;
  /**
   * Configuration for an IP target.
   */
  ip?: LoadBalancerTargetIp;
}

/**
 * Load Balancer type.
 */
export enum LoadBalancerType {
  /** `lb11` */
  LB11 = 'lb11',
  /** `lb21` */
  LB21 = 'lb21',
  /** `lb31` */
  LB31 = 'lb31',
}

/**
 * Type of the algorithm.
 */
export enum LoadBalancerAlgorithmType {
  /** `round_robin` */
  ROUND_ROBIN = 'round_robin',
  /** `least_connections` */
  LEAST_CONNECTIONS = 'least_connections',
}

/**
 * Protocol of the Load Balancer.
 */
export enum LoadBalancerServiceProtocol {
  /** `tcp` */
  TCP = 'tcp',
  /** `http` */
  HTTP = 'http',
  /** `https` */
  HTTPS = 'https',
}

/**
 * Type of the health check.
 */
export enum LoadBalancerServiceHealthCheckProtocol {
  /** `tcp` */
  TCP = 'tcp',
  /** `http` */
  HTTP = 'http',
}

/**
 * Type of the resource.
 */
export enum LoadBalancerTargetType {
  /** `server` */
  SERVER = 'server',
  /** `label_selector` */
  LABEL_SELECTOR = 'label_selector',
  /** `ip` */
  IP = 'ip',
}

/**
 * Props for {@link HtzLoadBalancer}.
 *
 * Manages a Hetzner Cloud Load Balancer.
 */
export interface HetznerLoadBalancer {
  /**
   * Name of the Load Balancer.
   */
  name: string;
  /**
   * Type of this Load Balancer.
   */
  loadBalancerType: LoadBalancerType;
  /**
   * Algorithm of the Load Balancer.
   */
  algorithm?: LoadBalancerAlgorithm;
  /**
   * Array of services.
   */
  services?: LoadBalancerService[];
  /**
   * Array of targets.
   */
  targets?: LoadBalancerTarget[];
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Enable or disable the public interface of the Load Balancer.
   */
  publicInterface?: boolean;
  /**
   * ID of the network the Load Balancer should be attached to on creation.
   */
  networkId?: number | IResolvable;
  /**
   * Name of network zone.
   */
  networkZone?: NetworkZone;
  /**
   * Location to create Load Balancer in.
   */
  location?: Location;
}

/**
 * L1 construct for a Hetzner LoadBalancer resource.
 *
 * Manages a Hetzner Cloud Load Balancer.
 */
export class HtzLoadBalancer extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Compute::LoadBalancer';

  /**
   * The `loadBalancerId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'loadBalancerId' }` at synthesis time.
   */
  public readonly attrLoadBalancerId: IResolvable;

  public name: string;
  public loadBalancerType: LoadBalancerType;
  public algorithm?: LoadBalancerAlgorithm;
  public services?: LoadBalancerService[];
  public targets?: LoadBalancerTarget[];
  public labels?: Record<string, string>;
  public publicInterface?: boolean;
  public networkId?: number | IResolvable;
  public networkZone?: NetworkZone;
  public location?: Location;

  constructor(scope: Construct, id: string, props: HetznerLoadBalancer) {
    super(scope, id, {
      type: HtzLoadBalancer.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrLoadBalancerId = this.getAtt('loadBalancerId');
    this.name = props.name;
    this.loadBalancerType = props.loadBalancerType;
    this.algorithm = props.algorithm;
    this.services = props.services;
    this.targets = props.targets;
    this.labels = props.labels;
    this.publicInterface = props.publicInterface;
    this.networkId = props.networkId;
    this.networkZone = props.networkZone;
    this.location = props.location;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      name: this.name,
      loadBalancerType: this.loadBalancerType,
      algorithm: this.algorithm,
      services: this.services,
      targets: this.targets,
      labels: this.labels,
      publicInterface: this.publicInterface,
      networkId: this.networkId,
      networkZone: this.networkZone,
      location: this.location,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- NetworkAttachment ---
/**
 * Props for {@link HtzNetworkAttachment}.
 *
 * Attaches a Server to a Network via the attach_to_network action.
 */
export interface HetznerNetworkAttachment {
  /**
   * ID of the Server to attach to the Network.
   */
  serverId: number | IResolvable;
  /**
   * ID of the Network to attach the Server to.
   */
  networkId: number | IResolvable;
  /**
   * IP to assign to the Server. If not provided, an IP will be auto-assigned.
   */
  ip?: string;
  /**
   * Additional IPs to assign to the Server.
   */
  aliasIps?: (string | IResolvable)[];
  /**
   * Subnet CIDR to auto-assign IP from. Must be a subnet of the Network.
   */
  ipRange?: string;
}

/**
 * L1 construct for a Hetzner NetworkAttachment resource.
 *
 * Attaches a Server to a Network via the attach_to_network action.
 */
export class HtzNetworkAttachment extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Compute::NetworkAttachment';

  public serverId: number | IResolvable;
  public networkId: number | IResolvable;
  public ip?: string;
  public aliasIps?: (string | IResolvable)[];
  public ipRange?: string;

  constructor(scope: Construct, id: string, props: HetznerNetworkAttachment) {
    super(scope, id, {
      type: HtzNetworkAttachment.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.serverId = props.serverId;
    this.networkId = props.networkId;
    this.ip = props.ip;
    this.aliasIps = props.aliasIps;
    this.ipRange = props.ipRange;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      serverId: this.serverId,
      networkId: this.networkId,
      ip: this.ip,
      aliasIps: this.aliasIps,
      ipRange: this.ipRange,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- PlacementGroup ---
/**
 * Define the Placement Group Type.
 */
export enum PlacementGroupType {
  /** `spread` */
  SPREAD = 'spread',
}

/**
 * Props for {@link HtzPlacementGroup}.
 *
 * Manages a Hetzner Cloud Placement Group.
 */
export interface HetznerPlacementGroup {
  /**
   * ID of the Placement Group. Assigned by the API on creation.
   */
  id?: number;
  /**
   * Name of the Placement Group.
   */
  name: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Define the Placement Group Type.
   */
  type: PlacementGroupType;
  /**
   * IDs of Servers that are part of this Placement Group. Read-only — managed by the engine at deploy time. Maps to the `servers` field in the Hetzner API response.
   */
  serverIds?: (number | IResolvable)[];
}

/**
 * L1 construct for a Hetzner PlacementGroup resource.
 *
 * Manages a Hetzner Cloud Placement Group.
 */
export class HtzPlacementGroup extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Compute::PlacementGroup';

  /**
   * The `placementGroupId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'placementGroupId' }` at synthesis time.
   */
  public readonly attrPlacementGroupId: IResolvable;
  /**
   * The `serverIds` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'serverIds' }` at synthesis time.
   */
  public readonly attrServerIds: IResolvable;

  public id?: number;
  public name: string;
  public labels?: Record<string, string>;
  public resourceType: PlacementGroupType;
  public serverIds?: (number | IResolvable)[];

  constructor(scope: Construct, id: string, props: HetznerPlacementGroup) {
    super(scope, id, {
      type: HtzPlacementGroup.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrPlacementGroupId = this.getAtt('placementGroupId');
    this.attrServerIds = this.getAtt('serverIds');
    this.id = props.id;
    this.name = props.name;
    this.labels = props.labels;
    this.resourceType = props.type;
    this.serverIds = props.serverIds;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      id: this.id,
      name: this.name,
      labels: this.labels,
      type: this.resourceType,
      serverIds: this.serverIds,
    } as unknown as Record<string, PropertyValue>;
  }
}


// --- Server ---
/**
 * Firewalls which should be applied on the Server's public network interface at creation time.
 */
export interface ServerFirewall {
  /**
   * ID of the Firewall.
   */
  firewall: number;
}

/**
 * Public Network options.
 */
export interface ServerPublicNet {
  /**
   * Attach an IPv4 on the public NIC. If false, no IPv4 address will be attached.
   */
  enableIpv4?: boolean;
  /**
   * Attach an IPv6 on the public NIC. If false, no IPv6 address will be attached.
   */
  enableIpv6?: boolean;
  /**
   * ID of the ipv4 Primary IP to use. If omitted and enable_ipv4 is true, a new ipv4 Primary IP will automatically be created.
   */
  ipv4?: number | null;
  /**
   * ID of the ipv6 Primary IP to use. If omitted and enable_ipv6 is true, a new ipv6 Primary IP will automatically be created.
   */
  ipv6?: number | null;
}

/**
 * Hetzner Cloud server type name. Available families:
 * - **Shared Intel (CX):** cx22 (2 vCPU / 4 GB), cx32 (4 vCPU / 8 GB), cx42 (8 vCPU / 16 GB), cx52 (16 vCPU / 32 GB)
 * - **Shared AMD (CPX):** cpx11 (2 vCPU / 2 GB), cpx21 (3 vCPU / 4 GB), cpx31 (4 vCPU / 8 GB), cpx41 (8 vCPU / 16 GB), cpx51 (16 vCPU / 32 GB)
 * - **Shared Arm64 (CAX):** cax11 (2 vCPU / 4 GB), cax21 (4 vCPU / 8 GB), cax31 (8 vCPU / 16 GB), cax41 (16 vCPU / 32 GB)
 * - **Dedicated vCPU (CCX):** ccx13 (2 vCPU / 8 GB), ccx23 (4 vCPU / 16 GB), ccx33 (8 vCPU / 32 GB), ccx43 (16 vCPU / 64 GB), ccx53 (32 vCPU / 128 GB), ccx63 (48 vCPU / 192 GB)
 */
export enum ServerType {
  /** `cx22` */
  CX22 = 'cx22',
  /** `cx32` */
  CX32 = 'cx32',
  /** `cx42` */
  CX42 = 'cx42',
  /** `cx52` */
  CX52 = 'cx52',
  /** `cpx11` */
  CPX11 = 'cpx11',
  /** `cpx21` */
  CPX21 = 'cpx21',
  /** `cpx31` */
  CPX31 = 'cpx31',
  /** `cpx41` */
  CPX41 = 'cpx41',
  /** `cpx51` */
  CPX51 = 'cpx51',
  /** `cax11` */
  CAX11 = 'cax11',
  /** `cax21` */
  CAX21 = 'cax21',
  /** `cax31` */
  CAX31 = 'cax31',
  /** `cax41` */
  CAX41 = 'cax41',
  /** `ccx13` */
  CCX13 = 'ccx13',
  /** `ccx23` */
  CCX23 = 'ccx23',
  /** `ccx33` */
  CCX33 = 'ccx33',
  /** `ccx43` */
  CCX43 = 'ccx43',
  /** `ccx53` */
  CCX53 = 'ccx53',
  /** `ccx63` */
  CCX63 = 'ccx63',
}

/**
 * Props for {@link HtzServer}.
 *
 * Manages a Hetzner Cloud Server.
 */
export interface HetznerServer {
  /**
   * Name of the Server to create (must be unique per Project and a valid hostname as per RFC 1123).
   */
  name: string;
  /**
   * Location to create the Server in (must not be used together with `datacenter`).
   */
  location?: Location;
  /**
   * **Deprecated**: This property is deprecated and will be removed after the 1 July 2026.
   */
  datacenter?: string;
  /**
   * Server type to create this Server with.
   */
  serverType: ServerType;
  /**
   * This automatically triggers a Power on a Server-Server Action after the creation is finished and is returned in the `next_actions` response object.
   */
  startAfterCreate?: boolean;
  /**
   * ID or name of the Image the Server is created from.
   */
  image: string;
  /**
   * ID of the Placement Group the Server should be in.
   */
  placementGroupId?: number | IResolvable;
  /**
   * SSH key IDs (`integer`) or names (`string`) which should be injected into the Server at creation time.
   */
  sshKeys?: (string | IResolvable)[];
  /**
   * Volume IDs which should be attached to the Server at the creation time. Volumes must be in the same Location.
   */
  volumes?: (number | IResolvable)[];
  /**
   * Network IDs which should be attached to the Server private network interface at the creation time.
   */
  networks?: (number | IResolvable)[];
  /**
   * Firewalls which should be applied on the Server's public network interface at creation time.
   */
  firewalls?: ServerFirewall[];
  /**
   * Cloud-Init user data to use during Server creation. This field is limited to 32KiB.
   */
  userData?: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Auto-mount Volumes after attach.
   */
  automount?: boolean;
  /**
   * Public Network options.
   */
  publicNet?: ServerPublicNet;
}

/**
 * L1 construct for a Hetzner Server resource.
 *
 * Manages a Hetzner Cloud Server.
 */
export class HtzServer extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Compute::Server';

  /**
   * The `serverId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'serverId' }` at synthesis time.
   */
  public readonly attrServerId: IResolvable;

  public name: string;
  public location?: Location;
  public datacenter?: string;
  public serverType: ServerType;
  public startAfterCreate?: boolean;
  public image: string;
  public placementGroupId?: number | IResolvable;
  public sshKeys?: (string | IResolvable)[];
  public volumes?: (number | IResolvable)[];
  public networks?: (number | IResolvable)[];
  public firewalls?: ServerFirewall[];
  public userData?: string;
  public labels?: Record<string, string>;
  public automount?: boolean;
  public publicNet?: ServerPublicNet;

  constructor(scope: Construct, id: string, props: HetznerServer) {
    super(scope, id, {
      type: HtzServer.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrServerId = this.getAtt('serverId');
    this.name = props.name;
    this.location = props.location;
    this.datacenter = props.datacenter;
    this.serverType = props.serverType;
    this.startAfterCreate = props.startAfterCreate;
    this.image = props.image;
    this.placementGroupId = props.placementGroupId;
    this.sshKeys = props.sshKeys;
    this.volumes = props.volumes;
    this.networks = props.networks;
    this.firewalls = props.firewalls;
    this.userData = props.userData;
    this.labels = props.labels;
    this.automount = props.automount;
    this.publicNet = props.publicNet;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      name: this.name,
      location: this.location,
      datacenter: this.datacenter,
      serverType: this.serverType,
      startAfterCreate: this.startAfterCreate,
      image: this.image,
      placementGroupId: this.placementGroupId,
      sshKeys: this.sshKeys,
      volumes: this.volumes,
      networks: this.networks,
      firewalls: this.firewalls,
      userData: this.userData,
      labels: this.labels,
      automount: this.automount,
      publicNet: this.publicNet,
    } as unknown as Record<string, PropertyValue>;
  }
}


// ==============================================================================
// Storage
// ==============================================================================

// --- Volume ---
/**
 * Props for {@link HtzVolume}.
 *
 * Manages a Hetzner Cloud Volume.
 */
export interface HetznerVolume {
  /**
   * Size of the Volume in GB.
   */
  size: number;
  /**
   * Name of the volume.
   */
  name: string;
  /**
   * User-defined labels (`key/value` pairs) for the Resource.
   */
  labels?: Record<string, string>;
  /**
   * Auto-mount Volume after attach. `server` must be provided.
   */
  automount?: boolean;
  /**
   * Format Volume after creation. One of: `xfs`, `ext4`.
   */
  format?: string;
  /**
   * Location to create the Volume in (can be omitted if Server is specified).
   */
  location?: string;
  /**
   * ID of the Server to attach the Volume to once it's created (Volume will be created in the same Location as the server).
   */
  serverId?: number | IResolvable;
}

/**
 * L1 construct for a Hetzner Volume resource.
 *
 * Manages a Hetzner Cloud Volume.
 */
export class HtzVolume extends ProviderResource {
  /** The CloudFormation-style type name for this resource. */
  public static readonly RESOURCE_TYPE_NAME = 'Hetzner::Storage::Volume';

  /**
   * The `volumeId` attribute of this resource.
   * Resolves to `{ ref: logicalId, attr: 'volumeId' }` at synthesis time.
   */
  public readonly attrVolumeId: IResolvable;

  public size: number;
  public name: string;
  public labels?: Record<string, string>;
  public automount?: boolean;
  public format?: string;
  public location?: string;
  public serverId?: number | IResolvable;

  constructor(scope: Construct, id: string, props: HetznerVolume) {
    super(scope, id, {
      type: HtzVolume.RESOURCE_TYPE_NAME,
    });
    this.node.defaultChild = this;
    this.attrVolumeId = this.getAtt('volumeId');
    this.size = props.size;
    this.name = props.name;
    this.labels = props.labels;
    this.automount = props.automount;
    this.format = props.format;
    this.location = props.location;
    this.serverId = props.serverId;
  }

  protected override renderProperties(): Record<string, PropertyValue> {
    return {
      size: this.size,
      name: this.name,
      labels: this.labels,
      automount: this.automount,
      format: this.format,
      location: this.location,
      serverId: this.serverId,
    } as unknown as Record<string, PropertyValue>;
  }
}

